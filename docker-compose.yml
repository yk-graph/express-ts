version: '3.9'

services:
  db:
    image: mysql:latest
    platform: linux/amd64
    container_name: express-ts-mysql
    ports:
      - 3306:3306
    volumes:
      - mysql:/var/lib/mysql
    env_file:
      - .env

volumes:
  mysql:
    name: express-ts-mysql
